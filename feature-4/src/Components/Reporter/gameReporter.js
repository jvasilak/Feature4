import React from 'react';
import './reporter.css';
// TODO: add a form that allows an admin user to create a new game entry
// in the database and submit it with the form
const GameReporter = () => {
    return (
        <div>
            <h1 className="pageHeader">Add new games</h1>
            <form>
            </form>
        </div>
    );
}

export default GameReporter;